apiVersion: v1
kind: Service
metadata:
  name: feedback-form
  namespace: psd
spec:
  type: ClusterIP
  selector:
    app: feedback-form
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
